# lefthook.yml
pre-commit:
  parallel: true
  commands:
    # 1. Format Code
    format:
      glob: "lib/**/*.dart"
      run: dart format {staged_files} && git add {staged_files}

    # 2. Lint Code
    analyze:
      glob: "lib/**/*.dart"
      run: flutter analyze